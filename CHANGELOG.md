# Revision history for Markovian

## 2026.9.3.0 — 2026-09-03

- Closed the compiled state-layout coercion boundary with nominal roles for state indexes, compiled states, MDPs, and MRPs. Added source-distribution compile-fail coverage for incompatible equality instances.
- Bounded `exactFiniteDist` at 4096 raw entries and added checked constructor work and rational-size limits with deterministic reports. Canonical duplicate aggregation now bounds the raw spine first. Added atomic checked bind limits for result support, work, numerator bits, and denominator bits, and routed Bayesian pushforward, conditioning, compiled policy closure, trace enumeration, and POMDP filtering through typed checked sequencing. Removed unrestricted distribution and kernel type-class composition.
- Completed exposed-declaration Haddock coverage, fixed source formatting and lint failures, and added pinned ShellCheck to CI. Archive projects now include `markovian-reverse` where required, and archive-only documentation builds through an isolated warning-free install before the coverage check.
- Removed the temporary neural reverse re-exports. Neural tests depend directly on `markovian-reverse`, and `denseReverseCircuit` is an explicit checked adapter to that backend-independent core.
- Added bounded effect-capable reverse execution with deterministic attempted-callback reports and a rank-2 host tensor adapter for closed `tanh` and pointwise multiplication. Moved public `Markovian.Tensor.Reverse` ownership to `markovian-tensor-reverse` through a narrow internal capability that retains atomic staged multi-output allocation. Added staged tensor allocation rollback, explicit session finalization, and deterministic allocation and cleanup fault injection. This does not establish generic tensor or CUDA lowering; D-067 is accepted only for the bounded effect and host-adapter scope.
- Added optional `markovian-safetensors` with a duplicate-preserving bounded parser and canonical encoder for the pinned metadata-free F64 profile. It validates complete allocation plans and exact payload coverage, preserves raw IEEE words, rejects unsupported dtypes and metadata, and includes malformed corpus, canonical golden, and compile-fail evidence. D-073 is accepted only for the pinned metadata-free F64 profile.
- Repaired release preparation integrity. Workflow revision input is validated before privileged jobs and independently in every job; shell commands consume quoted environment values only. Complete-bundle checksums and attestations now cover archives, SBOMs, the manifest, and source revision. Artifact manifests checksum every packaged semantic golden. SPDX output includes package verification codes and file license information and is checked by pinned independent tooling. Finalization uses atomic no-replace rename, archive CI uses checked extraction plus per-package dependency-closure consumers, component dependencies are bounded, and release-critical ownership is explicit. These repairs do not establish release readiness.
- Added non-publishing release preparation with checked package and API manifests, bounded source-archive validation, byte reproducibility checks, archive-only consumers, SHA-256 sums, SPDX 2.3 source SBOMs, atomic staging, and a least-privilege attestation workflow. Added migration, install, rollback, license, checklist, and release-note documentation. Release preparation remains non-publishing and human authorization remains separate.
- Expanded the book with foundation chapters for algebra, category theory, measure theory, categorical probability, information theory, categorical learning, polarity, push-pull duality, and game semantics. Added internal concept links, guided reading routes, direct primary-source links, an explicit law catalogue, derivations, executable fixtures, and derived categorical insights. Mathematical, pedagogical, and implementation-grounding reviews corrected the new chapter and improved the foundation narrative without removing its substantive content.
- Added checked approximate entropy, cross-entropy, KL-divergence, mutual-information, entropy-gradient, and fused cross-entropy-gradient references to `markovian-neural`, with product, decomposition, invariance, and finite-difference tests.
- Added typed parametric reverse circuits and a finite owned reverse-program interpreter to `markovian-neural`, with declared finite primal and cotangent layouts, structural parameter ownership, bounded preparation, opaque stored or recomputed tapes, explicit diagonal accumulation, exact law fixtures, compile-fail boundaries, deterministic reports, and all-coordinate finite differences under both tape policies.
- Replaced the list-only CUDA call with prepared F64 tensor matrix and matrix-VJP plans, structured device admission, UUID/ordinal selection, private context/module/stream ownership, safe synchronized transactions, retained cleanup diagnostics, and pre-user-launch-only fallback. Added CUDA-disabled and enabled differentials, boundary checks, a protected hardware workflow, and transfer-inclusive samples without a general device-correctness or performance claim.
- Repaired CUDA ownership and fallback transactions: executors are rank-2 scoped, teardown shares the execution lock, VJP retains first-launch commitment, cleanup failure independently prohibits fallback, and all signed kernel index products are admitted before launch. Added deterministic lifecycle fault injection, UUID-bound protected jobs, all-coordinate matrix finite differences, report goldens, and checked benchmark metadata and semantic checksums. Compute Sanitizer and hosted hardware remain release gates.
- Replaced mdBook's external MathJax 2 loader with a pinned local MathJax 3.2.2 SVG bundle. Added pre-typesetting recovery for Markdown-altered TeX, digest, local-loader, external-loader rejection, delimiter, generated-markup, and source-to-generated equation-count checks.
- Added a pinned mdBook documentation stack with a checked user and contributor book, local-link validation, CI builds, semantic guides, recipes, and project-reference appendices. Added a SHA-pinned GitHub Pages workflow and published the book at <https://josephjohncox.github.io/Markovian/>.
- Added validated floating and exact finite probability distributions.
- Added one-layer MRP and MDP interfaces with explicit transition rewards.
- Added validated policy closure with structured support errors.
- Added exact finite-horizon expected-return evaluation and trace enumeration.
- Added explicit seeded finite-support sampling with returned generator state.
- Added structured action, reward, successor, and stop-reason traces.
- Added validated exact finite compilation and unindexed-transition rejection.
- Added policy-free exact MDP compilation with separate compiled policy closure.
- Added bounded exact value iteration with residual and policy bounds, deterministic greedy extraction, and exact rational policy iteration.
- Added a synthetic bounded two-echelon serial-inventory fixture with conditioned geometric demand, exact finite-horizon backward induction, exhaustive duplicate-free base-stock comparison, checked widened-bound provenance, deterministic reporting, and a reproducible one-warm-up, twenty-sample benchmark.
- Added a separate Clark--Scarf (1960), Section III, exact finite-lattice specialization with source page/equation crosswalk, explicit lead-time timing and finite layouts, generic/direct/decomposed exact oracle agreement, zero-regret policy evaluation, retained-mass and widened-cap diagnostics, deterministic reporting, and a one-warm-up, twenty-sample reproducibility benchmark.
- Added Doğru's two-retailer balance assumption as a distinct bounded signed-action relaxation, with exact physical and relaxed oracles, balanced and physicalized policy evaluation, finite layouts and budgets, two-dimensional widening evidence, corrected thesis page crosswalk and event-order reporting, independent physical/relaxed recursion checks, deterministic reporting, and a reproducibility benchmark.
- Added a bounded exact two-stage fixed-batch model with physical batch feasibility, explicit supplier lead time, conditioned demand, complete finite layouts, finite-horizon oracle and constant-policy comparison, checked action/grid and demand-cap diagnostics, and separate stationary subsystem costs and discrete newsvendor inequalities. Stationary search uses explicit Cartesian `R1`/`R2` layouts, memoized and exactly charged law construction, retained parameter/domain provenance, and a checked finite/stationary separation report. The deterministic benchmark uses one excluded warm-up and twenty stable semantic samples.
- Added shared sampled-step and tabular foundations, pure TD(0), SARSA, Expected SARSA, and Q-learning updates, plus bounded resumable episodic runners.
- Added exact finite POMDP prediction, conditioning, filtering, and bounded belief planning.
- Added typed exact categorical syntax and dense rational CPU lowering.
- Initially added standard exact probability and Kleisli instances plus symmetric monoidal coherence and fanout combinators. The bounded checked API above now supersedes the unrestricted distribution and kernel instances.
- Added finite sets, nonempty finite objects, lawful scalar capabilities, and exact nonnegative rational scalars.
- Added opaque raw semiring matrices with tensor, biproduct, dagger, compact, and trace operations.
- Added normalized stochastic, proof-carrying deterministic, and exact convex matrix refinements with nominal roles and fixed exact coefficient validation.
- Added bounded exact delayed, proper first-exit, and nilpotent timed feedback fragments with opaque ownership witnesses, checked layouts, exact solve validation, reward-duration-output correlation, deterministic reports, rejected-cycle counterexamples, compile-fail boundaries, and a one-warm-up, twenty-sample benchmark. This does not add universal stochastic trace or cyclic open-system black-boxing.
- Added checked signed rational finite payoffs, exact payoff pullback through stochastic matrices, and exact state-payoff pairing laws.
- Added exact priors, positive supports, pushforward, joints, conditioning, prior-indexed Bayesian inversion, almost-sure equality, and checked Bayesian channels.
- Delegated exact POMDP prediction and conditioning to the shared Bayesian algebra without changing filtering behavior.
- Validate each exact distribution weight before duplicate aggregation, so a positive duplicate cannot hide a negative entry.
- Added recursive purity-indexed stochastic circuits with structural folds, explicit sharing and fanout, exact convex choice, and deterministic-only copy optimization.
- Added a raw-node-bounded circuit fold, deterministic static cost reports with caller-owned charges, opaque identity/reassociation/deduplication candidates, and exact endpoint-layout and row-major matrix certificate checks.
- Added exact circuit interpretation and dense CPU lowering through one nonnegative-rational matrix denotation, plus an explicit approximation boundary for floating and accelerator backends.
- Added first-order deterministic compilation for identity, composition, products, pairing, projections, and finite quoted tables.
- Added finite typed interfaces and directed hypergraphs, total type-preserving structure maps, explicit quotient pushouts, and checked cocone factorization.
- Added finite reachable acyclic alternating arenas, opaque legal histories, bounded receptive deterministic strategies, copycat, synchronized hidden-middle composition, and exact external-prefix observational equality without a full game-semantics claim.
- Added bounded total finite functions, concrete finite optics, owner-refined finite open games, exact rational decisions, pure contextual equilibrium enumeration, owner-preserving observational equality, deterministic reports, exhaustive binary-payoff differential evidence, and matching-pennies and non-credible-threat counterexamples.
- Added exact owned normal-form mixtures, separate CE and CCE devices, joint-outcome finite-horizon public-state stochastic-game evaluation, correlated-prior one-shot Harsanyi checks, bounded strategic-normal conversion, and checked closed-context open-game extraction. Added irrational and degenerate equilibrium boundaries, independent binary-game enumeration, compile-fail evidence, and a deterministic benchmark without claiming equilibrium existence or complete solving.
- Repaired the exact game boundary: active limits now revalidate stored rationals, sums and products use checked folds, correlation independence requires the same owned carrier, strategic-normal contingent-plan powers and products are preflighted before enumeration, and stochastic profile work uses capped arithmetic. Added Dirac-CE/pure-Nash, product-Nash/CE, mismatched-carrier, zero-mass-subcarrier, active-limit, one-below, and report-golden regressions. D-064 through D-066 were accepted after the complete release gates passed.
- Added structured-cospan composition, disjoint-union tensor, boundary reversal, commuting open-system cells, and double-interchange tests.
- Canonicalized interface-map rows and pushout class members, made cocone and middle-map checks layout independent, protected pushout witness roles, and kept circuit state orientation unchanged under topological boundary reversal.
- Protected `ExactBelief` state equality evidence with a nominal role and constrained pushout witness kinds.
- Added the canonical `sameFiniteLayout` operation to both finite-witness modules. Retained `sameFiniteSetLayout` and `sameFiniteObjectLayout` as descriptive aliases.
- Added directed circuit-decorated open topology while deferring arbitrary graph black-boxing, feedback, and continuous-time open-Markov claims.
- Added an opaque boundary-functional acyclic open-system refinement, finite named assignments, signature-keyed local circuits, aggregate purity, and exact live-frontier matrix semantics with explicit sharing and discard behavior.
- Corrected categorical copy to target the full tensor square with diagonal denotation.
- Added separate optional CUDA and framework-independent neural packages.
- Added optional restricted exact and numerical continuous-probability packages with affine-uniform moments, finite-observation conditioning, bounded GK15/7 quadrature, explicit SplitMix64 sampling, and resumable Welford Monte Carlo under proposed D-070 and D-071.
- Repaired continuous admission and execution boundaries: raw and infinite lists are bounded before canonicalization, zero entries are charged, rational posterior intermediates and quotients are checked, renaming must be injective, disintegration has one row-wide work account, floating widths and quadrature aggregates must be finite, and SplitMix64 output/state vectors are pinned against independent sampler formulas.
- Added bounded bivariate symbolic accounting with separate raw and canonical limits, deterministic reports, independent exact oracles, one-below failures, and an all-coordinate numerical differential fixture. D-070 and D-071 were accepted after the complete release gates passed.
- Repaired checked feedback preflight and evidence with checked cardinalities, queue-based exit reachability, one matrix-power sequence, and independent path oracles. Reports now include exact work and phase-specific rational maxima for discarded and retained values. Delayed and timed execution charge zero-mass branches before descent. Exact and one-below work and rational fixtures cover all three interpreters. D-069 was accepted after all compiler, archive, CI, and release gates passed.
- Added optional `markovian-autodiff` with a closed typed polynomial and `tanh` language, exact formal-polynomial and checked-Double execution, compiler-owned VJPs, bounded owned reverse lowering, opaque tapes, exact pairing laws, all-coordinate finite differences, and compile-fail boundaries under proposed D-068. This does not support arbitrary Haskell autodiff.
- Extracted the pure owned reverse foundation to optional `markovian-reverse`; neural and autodiff now depend downward on it. Added a separate bounded effect interpreter and rank-2 host adapter without claiming generic tensor or CUDA lowering.
- Repaired autodiff machine-index, scalar-work, and rational-growth admission; added a named Double policy, private bounded exact scalar SSA, independent syntax-recursive JVP oracle, and endpoint compile failures.
- Added a bounded `2 -> 2 tanh -> 2` two-layer fixture that checks every primal, input, weight, and bias coordinate for each output basis seed under stored and recomputed tapes against manual neural VJPs and independent central finite differences. The deterministic report is committed; this remains fixture evidence, not general neural lowering.
- Repaired tensor shape-product and pre-materialization preflight, replaced list-index matrix kernels, and added all-coordinate primitive finite differences, independent pairing, and a committed report golden.
- Added optional `markovian-tensor` with checked host F64 buffers, type-indexed shapes, immutable layouts and views, deterministic finite CPU primitives, distinct semantic ownership and storage identity, atomic payload/work preflight, and closed primitive VJP tapes under proposed D-072. This is not general tensor or device semantics.
- Added checked dense networks with manual VJPs, stable categorical derivatives, sized structural action masks, linear REINFORCE and actor-critic updates, FIFO replay, target synchronization, and atomic standard or Double-DQN batch updates.
- Added bounded exact-support bridge preflight, nominal action-witness roles, bounded gather/scatter overrun detection, and nonfinite scatter rejection.
- Added `markovian-neural-bridge`, which checks exact global action layout and neural head width, preserves each state's exact availability order, and represents terminal states without empty masks.
- Corrected the CUDA evidence record to label variable local measurements and avoid a general performance claim.
- Added CUDA benchmark warmup, raw-sample, range, and sample-standard-deviation reporting with a durable evidence record.
- Added reproducible toolchain, package, documentation, and CI checks.
- Split floating, sampled, learning, dense, benchmark, and old finite-IR sources out of the exact root. The reviewed integration graph now contains 16 packages, 18 test suites, 11 benchmarks, exact public sibling-edge checks, one public-module golden per package, package tests, and compile-fail boundaries. D-061 was accepted after all package, compiler, archive, documentation, and hosted gates passed.
- Reconciled four-package integration metadata and CI so the finite-game, reverse-program, and exact-support compile-fail boundaries and all four inventory semantic-report benchmarks run in the working tree and applicable unpacked source archives. Accepted D-054 through D-060 after the complete local matrix and hosted CI run `33467147313` passed on revision `993508f`.
