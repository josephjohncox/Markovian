# Historical control-status annotation — proof-assembly campaign

The final sentence of `/tmp/d081-retention-v2-B0ZMyODc/CLARIFICATIONS.md:42` says eager-item and post-commit interruption controls “remain unexecuted.” It is preserved historical wording, **not the current evidence status**. Neither that earlier file nor any authority/evidence was edited.

The retained v2 `batch-results-r1.json`, `BatchMain.hs`, `BatchProbe.hs` and source/revision records already show guarded O0/O2 controls: lazy items/layouts at batch IO return for k0/1/512, detection of the eager-item mutant, and injected synchronous post-publication failure for k0/1/2 with committed counters/IDs, no staged finalization before the caught failure, and k successful callbacks at session close. These concern the actual `allocatePayloads` publication path and subsequent output-spine force. They are **bounded synchronous** controls, not universal asynchronous callback-count or physical-reclamation proofs.

This assembly does not rerun those unchanged producer/ownership controls. New/affected numerical, local-Q, prefix-demand and bounded failure controls are in this campaign, with their own source identities. Reusing old evidence preserves its original source attribution, including the retained v2 unexpected pending-reshape assertion and its corrected r2 expectation.
