# Executable learning and bounded extensions: implementation checklist

## Objective

Implement all recommendations in [the durable review](../evidence/LEARNING-REVIEW-2026-09-05.md). The user explicitly requested this checklist and execution against it. Do not stop after planning or documentation alone.

Baseline: `7edc04a2d9a0de6af6fed1ccccb43303442210f5`.
Branch: `frontier/executable-learning`.

Status: **Active**. EL-01 contracts and EL-02 teaching (including the layout repair) passed independent review. EL-02's later resource experiment is now implemented with EL-06. EL-03 passed the supplied independent implementation review; its non-blocking opacity-probe defect is repaired and mutation-tested. EL-04 and EL-05 passed the supplied independent implementation reviews. EL-06 passed the supplied independent implementation/teaching review. EL-07 implementation-side integration is locally validated; final independent EL-07 review and parent completion audit remain outstanding. Every completion needs artifact paths and validation evidence in the execution log below. Keep incomplete items unchecked. A blocked item needs a concrete reason and next action. A passing focused test does not complete final integration.

## Invariants and scope

- Preserve released tag `v2026.9.3.0`, its target, and historical release evidence. No publishing or tag changes.
- Keep all 16 package versions at the current development baseline unless the user separately authorizes release preparation.
- Keep D-077 through D-085 Proposed. New contracts remain proposal-stage implementation evidence, not implicit acceptance.
- Freeze signatures, topology, ownership, limits, and failure precedence before each new semantic implementation.
- Prefer existing package ownership and no new package edges. Paired reports belong in `markovian-continuous`. Exact feedback sensitivity and finite fixed-policy aggregation belong in the exact root if their contracts fit that boundary. Escalate any need for a new package or edge.
- Retain exact Rational versus floating separation, checked canonical layouts, opaque nominal witnesses, bounded traversals, cumulative accounting, and atomic failure.
- Preserve reward/successor correlation and explicit terminal, reward, horizon, observation, and event timing.
- Make no new research-novelty, general optimization, general autodiff, continuous-policy, GPU, or production-training claim.
- Use one writer per checkout. Reviewers are read-only. No nested delegation or external publication by implementation agents.
- Preserve unrelated edits. Never terminate tmux or the parent session.

## EL-00 — Durable handoff

- [x] Record baseline, all findings, prior art, confidence, feature boundaries, and deferred universal claims.
- [x] Create this checklist before implementation.
- [x] Link this checklist from root TODO and include both durable files in source distributions via `Markovian.cabal` extra-doc-files.

## EL-01 — Documentation truth and feature contracts

- [x] Resolve F01-F08 in the review, including the autodiff readiness wording and Kleisli composition order.
- [x] Audit nearby current-guidance statements for the same contradictions. Preserve properly labeled historical evidence.
- [x] Add checked capability records separating availability, decision status, and evidence scope.
- [x] Generate capability presentation and validate package/module references. Do not equate current exposed-module snapshots with released snapshots.
- [x] Add negative tests for stale capability output, invalid status combinations/references, and current-versus-release confusion.
- [x] Freeze a bounded paired-difference API, ledger, rational limits, and failure precedence before implementation.
- [x] Freeze a narrow exact feedback JVP API, admitted directions, nominal layout/ownership, ledger, and failure precedence before implementation.
- [x] Freeze supplied-partition fixed-policy aggregation, observations/terminal/payoff rules, witness/quotient types, ledger, and failure precedence before implementation.
- [x] Obtain independent contract review and resolve concrete blockers before implementing EL-03 through EL-05.

## EL-02 — Executable teaching and learning routes

- [x] Classify every Haskell fence as runnable, compiled-source fragment, or explicit pseudocode. Explain the convention.
- [x] Add a manifest/checker that rejects unclassified, missing, stale, or invalid references. Test the checker with deliberate failures.
- [x] Compile and execute runnable examples through existing components where practical. Fragments need compiled source context. Do not disguise runnable examples as pseudocode to bypass coverage.
- [x] Generate stable displayed outputs from executed fixtures and reject stale outputs. Wire validation into source CI and source archives.
- [x] Add three reading routes: MDP practice, probability/composition, differentiation/backends.
- [x] Add prerequisites, learning outcomes, and next steps for the routes and new lessons.
- [x] Add an orientation/notation guide: kernel and matrix orientation, composition, timing, exact/approximate/layout equality, and distinct reverse operations.
- [x] Add a law-laboratory chapter with precise hypotheses, commands, checked results, hints, exercises, and checked solutions.
- [x] Lab A: shared fair draw versus independent executions, plus deterministic copy-law contrast.
- [x] Lab B: matrix transpose versus prior-indexed Bayesian inversion and zero-evidence boundary.
- [x] Lab C: joint reward/successor law versus correlation loss. Explain which observables distinguish them.
- [x] Lab D: terminal payoff versus horizon truncation and event timing.
- [x] Lab E: exact equality versus floating reassociation, with a frozen operation order.
- [x] Lab F: logical tensor coordinates versus storage identity. Distinguish primitive-on-view from view-to-base derivatives.
- [x] Add a bounded resource-admission experiment with EL-06, reusing the checked-bind distinction rather than claiming a Monad instance.
- [x] Review executable examples for circular oracles and claims that exceed their fixtures.

## EL-03 — Exact paired-difference report

Depends on the reviewed EL-01 contract.

- [x] Implement the report in the existing continuous exact package with no numerical/runtime dependency.
- [x] Return exact means, variances, covariance, expected difference, difference variance, independent-marginal variance baseline, and a signed comparison.
- [x] Use one operation-wide preflight and cumulative ledger for all moments and derived rational arithmetic. Return no partial report on failure.
- [x] Test positive, zero, and negative covariance, shared and independent owners, zero variance, signed coefficients, and reordered layouts where applicable.
- [x] Check all reported identities against an independent exact polynomial/moment oracle.
- [x] Test exact and one-below limits, rational growth, saturation, and deterministic failure precedence.
- [x] Add the U versus U and U versus 1-U lessons. Check exact values 0, -1/12, 1/3, and 1/6 in their stated roles.
- [x] Update public snapshots, changelog, book, capability record, and source-distribution membership without changing immutable published history.

## EL-04 — Exact feedback sensitivity

Depends on the reviewed EL-01 contract.

- [x] Implement the narrow directional JVP, not general autodiff or a full Jacobian. Fixed-topology reward directions are an admissible first slice if named explicitly.
- [x] Evaluate derivatives of real parameterized semantics at rational points. Retain strict discount and lawful direction requirements.
- [x] Check differentiated fixed-point equations literally. Meter all arithmetic and solves with one ledger and atomic failure.
- [x] Add an independent derivative oracle, such as direct dual-number finite unrolling or symbolic small fixtures. Do not reuse production derivative code.
- [x] Teach retry value V=4/3 and dV/dp=8/9 at gamma=p=1/2. If probability directions are outside the first API, label that symbolic exercise separately from API coverage.
- [x] Compare finite-unrolling derivatives with fixed-point sensitivity without claiming finite equality in general.
- [x] Test zero and boundary cases within the declared domain, layout/role rejection, exact and one-below budgets, rational growth, and failure precedence.
- [x] Update public snapshots, changelog, book, capability record, compile-fail gates where needed, and source archives.

## EL-05 — Checked supplied-partition state aggregation

Depends on the reviewed EL-01 contract.

- [x] Implement a checker for a supplied finite partition of a fixed-policy model. Do not implement automatic partition search.
- [x] Preserve terminal behavior, applicable terminal payoffs, declared observation labels, and the joint immediate-reward/successor-block distribution.
- [x] Return an opaque checked quotient or a concrete distinguishing witness. Bound witness construction and validation.
- [x] Preserve original finite layouts and explicit quotient mapping. Reject incomplete/duplicate/unknown partition entries.
- [x] Compare original and lifted quotient finite-horizon evaluations and admitted reward/observation trace distributions with independent small fixtures.
- [x] Add equal-expected-reward but different reward-law rejection, terminal mismatch, observation mismatch, and reward/successor correlation counterexamples.
- [x] Test exact and one-below state/block/support/work/rational limits and atomic failure. Test ownership/opacity at compile time where needed.
- [x] Add a runnable aggregation lesson with a successful quotient and a failed partition witness.
- [x] Update current public snapshots, changelog, book, capability record, and source archives.

## EL-06 — Resource-aware optimization contract

This item resolves D-085's contract. It does not implement the full cache proposal.

- [x] Distinguish denotation, admission, error precedence, and execution cost in an explicit contract.
- [x] Choose and document how source semantic work differs from interpreter execution work, if both are reported.
- [x] Resolve the 100-unit source versus 1-unit cache-hit example under a 10-unit budget. Do not promise identical failures while silently changing admission policy.
- [x] Explain cache construction, hits, semantic-limit validation, and callback purity/failure assumptions. Keep arbitrary closure identity excluded.
- [x] Add a checked worked experiment illustrating the selected relation and its boundary.
- [x] Amend D-085's proposal consistently and retain its Proposed status.

## EL-07 — Integration and independent evidence

- [x] Run focused tests and checker negative tests for each completed phase. Record actual outcomes, not planned commands.
- [x] Run warning-enabled full build and all applicable test suites. Report current suite counts from actual output.
- [x] Run Fourmolu, HLint, cabal-fmt, relevant Python/shell checks, package manifests, release metadata/policy, and new gates.
- [x] Run relevant compile-fail boundaries using exact current inplace package units.
- [x] Run book, local-link, MathJax semantic-rendering, capability, snippet, and generated-output checks.
- [x] Check new public Haddock signatures for hidden implementation types. Use the repository's authoritative warning-enabled documentation procedure.
- [x] Check source archives include all new code, fixtures, manifests, scripts, generated learning artifacts, and durable records. Validate archive-only examples where practical.
- [ ] Obtain independent correctness and teaching reviews. Fix concrete blockers and re-run affected gates.
- [x] Audit each checkbox against concrete evidence. Keep unresolved work unchecked and document residual risks.
- [x] Produce a final summary and clean intended commits. External PR, merge, or publication remains parent-controlled and separately reported.

## Execution log

### 2026-09-05 — Baseline capture

- Recorded the user's full request and reviewed findings before implementation.
- Confirmed clean baseline and created `frontier/executable-learning`.
- All three post-merge runs for PR #5 passed. No runtime feature evidence is inferred from those older checks.
- Next action: complete EL-00 linking, then EL-01 documentation truth and frozen contracts.

### 2026-09-05 — EL-00 verification and EL-01 implementation (independent review pending)

- Corrected F01–F08 in `README.md`, the API map, introduction, tensor, autodiff and kernel chapters. Nearby current guidance repairs include `MIGRATION.md`, `docs/CONTEXT.md`, `release/README.md`, and the feedback/evidence chapters. Historical release evidence and D-037/D-061 history were not rewritten.
- Added nine checked records in `docs/capabilities/current.json`, digest-pinned selected immutable module-membership evidence in `docs/capabilities/released-modules.json`, and generated `docs/book/src/capabilities.md`. `scripts/check-capabilities` checks current Cabal/snapshot references separately from immutable membership; `--verify-release` additionally checks the released Git source and tag target. These records are selected capability claims, not a complete signature inventory or automatic semantic proof.
- Added `scripts/test_capabilities.py` (20 tests, including stale output, reference/status errors, current-versus-release substitution and no-Git archive-layout validation). Wired ordinary capability checking into the book gate and CI metadata step, with negative tests in CI. Root source membership includes the checker, tests, records, presentation, and all new contracts.
- Froze full future declarations and bounded algorithms in [EL-03 paired difference](EL-03-PAIRED-DIFFERENCE.md), [EL-04 event-reward JVP](EL-04-REWARD-JVP.md), and [EL-05 aggregation](EL-05-AGGREGATION.md). No Haskell semantic implementation or public export changed. The JVP holds probabilities and discount fixed; dV/dp=8/9 is explicitly a separate symbolic exercise. Aggregation owns a checked quotient table instead of bypassing compiled-model opacity.
- Selected source-semantic admission with separate executor cost for D-085 in [the EL-06 policy](EL-06-RESOURCE-ADMISSION.md), amended the Proposed decision, and resolved the 100/1/10 example. EL-06's checked experiment and concrete cache API remain unimplemented; its checkboxes remain open.
- Commands/results: `python3 scripts/check-capabilities --write --verify-release` generated nine rows and verified immutable membership; `python3 scripts/test_capabilities.py` passed 20 tests; `python3 scripts/check-capabilities --verify-release` passed. Initial checker validation incorrectly required Python's case-sensitive order for current snapshots; corrected to compare sorted contents without modifying snapshots, then reran successfully.
- Commands/results: `bash scripts/check-package-manifest`, `bash scripts/check-release-metadata`, `bash scripts/check-release-policy`, and `python3 scripts/test_release_tool.py` passed: 16 packages, 18 Haskell suites, 11 benchmarks, two policy tests and 32 release-tool tests. Those suite counts are manifest checks, not a new Haskell-suite execution claim.
- Commands/results: `bash scripts/check-book` passed with mdBook 0.5.4, local MathJax 3.2.2, 45 Markdown files and 419 checked display-math blocks. Generated HTML: `docs/book/build/index.html` (ignored build artifact).
- Commands/results: `cabal check`, `cabal-fmt --check Markovian.cabal`, `ruff check scripts/check-capabilities scripts/test_capabilities.py`, `shellcheck -x -P SCRIPTDIR scripts/check-book`, `python3 -m py_compile scripts/check-capabilities scripts/test_capabilities.py`, and `git diff --check` passed. Initial Ruff failures (import order, unused import, executable bits) were corrected. Initial ShellCheck invocations without the script-relative include path failed SC1091; the explicit `-P SCRIPTDIR` invocation passed. An initial read-only decision-heading inspection used a wrong numeric slice, was corrected, and made no edits.
- Commands/results: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el01-sdist` produced all 16 development source archives (not release candidates). A Python `tarfile` membership assertion checked 12 required EL-00/EL-01 root paths and the existing root TODO link. Root archive: `dist-newstyle/el01-sdist/Markovian-2026.9.3.0.tar.gz`. Refreshed all archives after the final source edits. A separate Python run reused `release_tool.validate_archive` and `extract_archive` for all 16 archives, assembled their original package layout in a temporary directory without `.git`, and passed the archive-only capability gate plus all 20 negative tests. No Haskell archive compilation is inferred. A later `cabal sdist Markovian` refresh was rejected as a component target (Cabal-7151); the supported `cabal sdist all` command was used instead.
- An explicit Python/Git invariant check confirmed all 16 versions unchanged from durable-plan commit `69382b2`, D-077–D-085 still Proposed, released tag object `d746952084e09647e7bcd67b92dd6cef9d0e14c9` and target `fe6abb8db9b3def65ead6602168eef860a79527c` unchanged. Metadata checks also confirmed no package/dependency-edge drift.
- Structured stage evidence destination: `/home/josephcox/.pi/agent/sessions/--home-josephcox-dev-Markovian--/subagent-artifacts/outputs/c4bb5762-3f07-456b-bae3-7564338015d5/stages/truth-contracts-write.json`.
- Remaining work: independent EL-01 contract/correctness review (no implementation stages independently reviewed yet), EL-02 executable lessons, EL-03–EL-05 semantic implementations after review, the EL-06 checked experiment, and EL-07 full integration. No full Haskell build/test/Haddock/hardware campaign was run for this documentation/Python-only stage. All package versions, dependency edges, released tag/history and D-077–D-085 statuses remain unchanged.

### 2026-09-07 — Handoff protocol recovery

- Workflow `c4bb5762-3f07-456b-bae3-7564338015d5` stopped after `truth-contracts-write`. The child committed `2d3140e` but omitted the runtime-required `structured_output` call. Writing a JSON artifact did not satisfy that protocol.
- Parent verified branch `frontier/executable-learning`, the committed implementation, and a clean working tree. No rollback or duplicate implementation is needed.
- Parent reran `python3 scripts/test_capabilities.py` (20 passing tests), `python3 scripts/check-capabilities --verify-release` (nine checked records), and `git diff --check` (passed).
- Resume at independent EL-01 contract review. Later semantic implementation remains gated by that review. Recovery uses the same native subagent protocol, ordinary writer handoffs, and explicit JSON reviewer verdicts checked by the orchestration code. Malformed verdicts fail closed.
- No decision is accepted by this recovery. EL-02 through EL-07 remain incomplete.

### 2026-09-07 — EL-01 independent review recorded; EL-02 executable teaching

- Received the prior read-only independent contract review, with no findings. Its exact JSON evidence is recorded below. The reviewer did not independently rerun commands; the recovery entry records the parent's 20-test and release-verification rerun. This closes only the EL-01 review gate, not later feature implementation or teaching review.

```json
{"verdict":"passed","summary":"No issues found. Reviewed F01–F08, nearby guidance, capability records/checker/generated output, all 20 test cases, immutable-membership and archive handling, and all three semantic contracts plus D085 admission policy against existing source. Mathematical conditions, ownership, timing, cumulative budgets, atomic failure, and independent-oracle requirements are sufficient for subsequent implementation. Later phases correctly remain incomplete. This was a read-only source/contract review; commands were not independently rerun. Confidence: high for the reviewed contracts.","findings":[]}
```

- Added `docs/learning/fences.json` and `scripts/check-learning`: 73 Haskell fences including the decisions Markdown include; three complete runnable contexts, 67 compiled fragments, and three explicitly reasoned, digest-pinned conceptual/historical sketches. The checker checks actual Cabal component/module/source-directory membership, source excerpts, includes/ranges, full runnable contexts and output receipts. Whitespace is normalized for fragment matching; compilation is of the complete registered source, not of a stand-alone excerpt. Static checks deliberately do not claim execution.
- Added five `*Contexts.hs` modules to existing test components (root context under `docs/learning`, others in dense-exact, learning, neural and GPU). Preserved the real calls rather than reclassifying them as pseudocode. Compiling exposed real error-type mismatches in the prior Bayesian push/evidence, dense lowering/run and neural forward/optimizer snippets; explicit checked IO conversion now joins those failures. The GPU preparation and execution snippets have separate compiled contexts, without a hardware-execution claim. Teaching wrappers infer constraints and retain named intermediate bindings; only missing-signature warnings and the specific HLint simplifications that would erase those fragments are suppressed.
- Added `test/LawLaboratory.hs` (A–D) and `packages/markovian-tensor/test/TensorLawLaboratory.hs` (E–F). Both run in their normal suites and via `--learning`; no package, component, public export or dependency edge was added. A checks actual shared/independent stochastic matrices and a deterministic witness. B checks transpose, supported inversion, posterior masses and `ZeroEvidence`. C checks a hand-enumerated cross moment, equal additive MDP values and distinct trace supports. D checks terminal-before-horizon values, stop reasons and realized transition counts. E executes separate F64 tensor additions and compares Rational arithmetic. F checks storage, logical order, a real multiply tape, explicit transpose pullback and independent six-coordinate quadratic differences. No automatic view-to-base tape is claimed.
- Added the three learner routes, orientation guide and law laboratory; linked them in navigation/introduction and first-MDP next steps. Each new route/lesson states prerequisites, outcomes and next steps. Exercises have hints and answers checked by the fixtures. The laboratory identifies independent reference tests and explains why materialize-first equivalence alone is not an independent oracle. Self-review found no production-oracle circularity; independent teaching review is still required.
- Generated three displayed stdout files and `docs/learning/output-receipt.json` by actual execution, including the first MDP's output. Receipts bind implementation/context sources, Cabal files, manifest/checker and output bytes. Wired the static gate into `check-book`, execution and 30 deliberate-failure tests into source CI, and archive-only checking into the source-distribution CI step. `scripts/check-learning-archive` validates all 16 archives, reconstructs the original package layout without Git, invokes archived tools, builds contexts, executes examples and runs checker tests. Cabal automatically includes the new test modules; root extra-doc/source membership includes manifest, receipt, outputs and scripts.
- Commands/results: a clean `cabal build all --project-file=cabal.project.ci` passed on GHC 9.8.4 with the project's warning-as-error configuration. `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed all 18 suites (276 `PASS:`-named checks, plus suites with their own output conventions and the new asserted laboratory outputs). These counts are actual execution, not manifest counts. Earlier default-cache builds failed with stale optimized/nonoptimized inplace interface/linker references; forcing recompilation did not repair the shared cache. Moving the ignored `dist-newstyle` cache to `/tmp/markovian-el02-old-dist-*` and rebuilding cleanly fixed the issue; no source workaround was used. Both affected context files subsequently received clean Haskell diagnostics. An isolated seven-component teaching build had already passed independently of that stale cache.
- Commands/results: `python3 scripts/check-learning --write` executed and generated the outputs; `python3 scripts/test_learning.py` passed 30 tests. The negatives cover missing/unclassified/stale fences, bad classes, unknown/escaping/missing sources, wrong package/component/module membership, includes/ranges, stale pseudocode, output and implementation receipts, missing execution and duplicate outputs. Three real temporary Cabal/GHC cases test rejected ill-typed context, runtime fixture failure and stale executed answer (followed by successful regeneration). Those deliberate compiler/runtime failures are expected passing test evidence, not hidden gate failures.
- Commands/results: `bash scripts/check-book` passed: mdBook 0.5.4, local MathJax 3.2.2, 48 Markdown files, 419 source-to-generated display-math blocks, local links/includes and static learning/capability checks. No new equations increased the previous math-block count. An additional direct Node attempt to load the browser MathJax bundle failed because `xmldom-sre` is not installed; no browser/MathJax-startup execution is claimed by this stage. The repository's existing HTML/MathJax semantic gate passed; full browser rendering remains an EL-07 integration gate.
- Commands/results: package manifest, release metadata and release policy checks passed with all 16 packages, 18 suites and 11 benchmarks unchanged; the 20 capability tests and `check-capabilities --verify-release` passed. `cabal check`, focused Fourmolu checks and HLint passed; executable-bit Ruff errors were fixed and the new Python files passed Ruff. `git diff --check` passed. No benchmark execution, Haddock campaign, GHC 9.4.8, CUDA execution or hosted CI run is claimed here.
- Commands/results: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el02-sdist` created 16 development archives. `python3 scripts/check-learning-archive dist-newstyle/el02-sdist` passed archive-only context compilation, all three executions/output comparisons and all 30 checker tests. Root archive is `dist-newstyle/el02-sdist/Markovian-2026.9.3.0.tar.gz`. Archives are refreshed after final documentation/evidence edits; immutable release archives/history are untouched.
- Remaining work: independent EL-02 correctness/teaching review; EL-03–EL-05 implementations against the reviewed contracts; EL-06 checked resource experiment; final EL-07 integration including warning-enabled public Haddock and actual browser rendering. The resource-experiment checkbox remains deliberately open. All versions and D-077–D-085 Proposed statuses remain unchanged. No push, PR, publication, merge, tag operation or nested delegation occurred.

### 2026-09-07 — EL-02 timeout recovery

- Workflow `4241ef7e-c057-4d85-b655-4069bbcd1247` passed independent EL-01 review, then its teaching writer `753db200-75a3-48e5-a0b7-2fd0a4085de6` reached the default 1,800-second deadline. This timeout is not a completed EL-02 review or integration gate.
- Parent inspected the partial tracked/untracked changes, checker, output receipt, execution log, and prior run status. No prior worker or Cabal/GHC build process remained active at inspection.
- Preserved the tracked binary diff and untracked files in `/tmp/markovian-el02-recovery-ot5Tcv/tracked.patch` and `/tmp/markovian-el02-recovery-ot5Tcv/untracked.tar.gz`. These are temporary recovery copies, not release artifacts.
- Parent independently reran `python3 scripts/check-learning` (73 statically classified fences and current output receipt) and `python3 scripts/test_learning.py` (30 passing tests, including deliberate compilation/runtime failures). Static checking is not a claim that the parent recompiled all real teaching contexts.
- Resume the partial implementation rather than overwrite it. Finish EL-02 validation and handoff, then require an independent teaching review. Use explicit per-stage deadlines instead of the accidental 30-minute default. Do not spend the delivery window rebuilding valid caches solely to silence inconclusive editor diagnostics.
- EL-03 through EL-07 remain incomplete. Later work continues only after the relevant stage review passes.

### 2026-09-07 — Native runner dependency recovery

- Workflow `6f0884e5-5616-4461-a8a6-b64193900aad` failed before its resumed child `d6cabe6e-dc71-4f93-a107-a98fa2f81340` became ready. No child session was persisted; the runtime observed process exit code 1. No implementation completion is inferred.
- The ten-second startup timeout masked `MODULE_NOT_FOUND` for `@earendil-works/pi-tui`, imported by the installed `pi-subagents` watchdog. Evidence: `/tmp/pi-subagents-uid-1000/async-subagent-runs/d6cabe6e-dc71-4f93-a107-a98fa2f81340/runner.stderr.log` and `process-terminal.json`.
- Parent verified the peer was missing from runner resolution but present in the installed Pi runtime. Added only `/home/josephcox/.pi/agent/npm/node_modules/pi-subagents/node_modules/@earendil-works/pi-tui`, a symlink to `/home/josephcox/.nvm/versions/node/v26.1.0/lib/node_modules/@earendil-works/pi-coding-agent/node_modules/@earendil-works/pi-tui`. No download, package manifest/settings change, or Markovian dependency change was made.
- A Node resolution/import check from the runner's package root now resolves that existing package and loads its `Text` export. This checks the missing dependency, not an entire workflow. Retry the native protocol with a fresh writer context and preserved working tree; independent review remains mandatory.

### 2026-09-07 — EL-02 resumed implementation and validation

- Inspected every tracked diff and untracked teaching artifact, the full durable review/checklist, and the recovered execution logs. Preserved the timed-out implementation rather than recreating it. `/tmp/el02-clean-build.log`, `/tmp/el02-all-tests.log`, `/tmp/el02-learning-write.log`, and `/tmp/el02-archive.log` substantiate the earlier build, 18-suite run (276 `PASS:` labels), teaching execution and archive-only compilation claims. `/tmp/el02-hlint.log` contains an earlier 45-hint failure, not passing evidence; the fresh HLint run below supplies the passing result.
- Strengthened two existing fixture assertions: laboratory B now checks both complete supported Bayesian-inverse rows `[[2/5,3/5],[0,1]]`, rather than only their count; laboratory E directly checks the rounded `b+c` intermediate used by its exercise answer. Regenerated outputs by execution and updated the matching lesson text. Added all three learning Python scripts to CI's explicit syntax-check list. No runnable fence was reclassified, and all six experiments still exercise real public APIs with literal independent expectations or base-coordinate differences.
- Fresh compiler evidence: `cabal build all --project-file=cabal.project.ci` and `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed after the final fixture edits, with 18 passing suites and 276 `PASS:` labels. Logs: `/tmp/el02-resume-build.log`, `/tmp/el02-resume-tests.log`. Used the existing cache; no cache rebuild or source workaround was needed. Editor analysis of the two edited Haskell files was unavailable, not clean; actual GHC 9.8.4 warning-as-error compilation supplies validation.
- Fresh teaching evidence: `python3 scripts/check-learning --write`, then `--run`, passed all context compilation and three complete output comparisons; 73 fences remain classified (three runnable, 67 fragments, three explicit pseudocode sketches). `python3 scripts/test_learning.py` passed all 30 tests, including the expected real compiler/runtime rejection fixtures. Logs: `/tmp/el02-resume-learning-write.log`, `/tmp/el02-resume-learning-run.log`, `/tmp/el02-resume-checker-tests.log`. `docs/learning/output-receipt.json` and the three displayed output files are current executed artifacts, not hand-edited answers.
- Fresh presentation/format evidence: `bash scripts/check-book` passed 48 book files, local links/includes, static capability/learning checks, and 419 source-to-generated MathJax display blocks (`/tmp/el02-resume-book.log`). Full repository HLint, including `docs/learning`, passed with no hints (`/tmp/el02-resume-hlint.log`); Fourmolu checked every `.hs` under the existing CI source roots plus `docs/learning`. Changed Cabal files passed `cabal-fmt --check`; the three learning Python scripts passed Ruff and `py_compile`; `scripts/check-book` passed `bash -n` and ShellCheck with `-x -P SCRIPTDIR`; `cabal check` and `git diff --check` passed.
- Fresh metadata evidence: package-manifest, release-metadata and release-policy checks passed (16 packages, 18 suites, 11 benchmarks, unchanged dependency edges; two policy tests). Capability release verification and all 20 capability tests passed; all 32 release-tool tests passed. An explicit Python/Git invariant check confirmed all 16 package versions unchanged, D-077–D-085 still Proposed, and the released tag object/target unchanged.
- Fresh archive evidence: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el02-sdist` produced all 16 development archives; `python3 scripts/check-learning-archive dist-newstyle/el02-sdist` passed archive-only component compilation, all three executions/freshness comparisons, and all 30 checker tests without Git or checkout symlinks. Logs: `/tmp/el02-resume-sdist.log`, `/tmp/el02-resume-archive.log`. Root artifact: `dist-newstyle/el02-sdist/Markovian-2026.9.3.0.tar.gz`. After the evidence-only checklist update, refreshed all 16 archives and verified byte-for-byte membership of all 46 changed/new distributed files (CI YAML is not a source-distribution input); archived executable sources and outputs are unchanged by that evidence edit.
- Independent EL-02 teaching/correctness review remains mandatory after this writer. EL-03 was not started. EL-06's resource experiment and EL-07's later integration/review gates remain unchecked; no browser-startup, Haddock, GHC 9.4.8, CUDA, benchmark, hosted-CI, release or publication result is inferred from this host-only stage.

### 2026-09-07 — EL-02 independent-review layout repair

- Started from clean committed teaching implementation `66c678748bb4bd37f8daf3a53bda87778bdd888f`; preserved it rather than recreating the timed-out writer's work. Read the full durable review/checklist and inspected the existing resumed build, test and archive logs. The previous 18-suite execution remains prior evidence, not a new full-suite run in this repair.
- Independent teaching review was **blocked** by P1 at `sampling-learning.md:193–199`: displayed `qLearningConfig` arguments aligned with the `config` declaration, while the registered wrapper had valid layout. The review otherwise found the six real-API laboratories and independent expectations sound. Its read-only assessment did not independently execute commands. This repair does not self-award a passing independent review.
- Reproduced the defect: the committed checker accepted the malformed regression fixture; the repaired checker rejects it. An actual GHC 9.8.4 regression compilation reports a parse error for that displayed layout, then the corrected fixture compiles and prints the independent expected sum `15`. Indented the five displayed arguments correctly, retaining the compiled-fragment classification.
- The checker now compares the relative column ordering of displayed line starts against the corresponding source words, while allowing surrounding context, different indentation widths and line wrapping. This is a bounded layout-sensitive excerpt check, not a Haskell parser; complete registered contexts still supply compiler evidence. Updated the documented convention. No fence classes or laboratory expectations changed: 73 fences remain three runnable, 67 fragments and three explicitly reasoned pseudocode sketches.
- Added four regression tests (valid width variation, the exact declaration-column failure, changed relative layout with identical words, and real malformed/corrected compiler executions). `python3 scripts/test_learning.py` passed all 34 tests after final checker edits (`/tmp/el02-repair-tests.log`). Expected compiler/runtime failures are asserted test cases. The initial Ruff check found SIM102 in a nested condition; combined the conditions and reran successfully. `ruff check scripts/check-learning scripts/test_learning.py`, `python3 -m py_compile scripts/check-learning scripts/test_learning.py`, and `git diff --check` passed.
- `python3 scripts/check-learning --write`, `--run`, and the static gate passed; the eight registered Cabal targets include the root library, six test components and the sample executable. All three output comparisons passed. Regeneration changed only the receipt's source digest, not displayed stdout. Logs: `/tmp/el02-repair-learning-write.log`, `/tmp/el02-repair-learning-run.log`. `bash scripts/check-book` passed 48 files, local links/includes, capability/learning checks and 419 source-to-generated display-math blocks (`/tmp/el02-repair-book.log`). Package-manifest, release-metadata and release-policy checks passed with 16 packages, 18 suites and 11 benchmarks unchanged; the two release-policy tests passed.
- `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el02-sdist` and `python3 scripts/check-learning-archive dist-newstyle/el02-sdist` passed. Archived tools rebuilt contexts, executed all three fixtures and passed all 34 checker tests without Git or checkout symlinks. Logs: `/tmp/el02-repair-sdist.log`, `/tmp/el02-repair-archive.log`; root artifact: `dist-newstyle/el02-sdist/Markovian-2026.9.3.0.tar.gz`. Refreshed archives after this evidence-only log edit and checked byte-for-byte membership of the six repair files; executable inputs are unchanged by that log edit.
- Remaining gates are unchanged: independent EL-02 re-review is required; EL-06's resource experiment and EL-07's later integration remain pending. EL-03 was not started. No new full 18-suite run, public Haddock, browser-startup, GHC 9.4.8, CUDA, benchmark or hosted-CI execution is claimed by this repair. Package versions, dependency edges, Proposed D-077–D-085 and released tags/history are untouched; no harness settings/packages or external publication operations changed.

### 2026-09-07 — EL-02 re-review recorded; EL-03 paired-difference implementation

- Received the supplied independent re-review of repair `8c6ec2a`: `{"verdict":"passed","summary":"No issues found. Reviewed repair 8c6ec2a and its teaching/checker blast radius. The Q-learning layout defect is corrected without reclassification; regression tests cover rejection and actual malformed/corrected GHC compilation. Inspected logs substantiate 34 passing checker tests, teaching execution, book checks and archive-only validation. Six laboratories retain independent expectations and bounded claims. EL-06 and later integration remain explicitly pending. Read-only source/log review; commands were not independently rerun. Confidence: high.","findings":[]}`. This closes EL-02's re-review, not later semantic review.
- Implemented the reviewed EL-03 surface in `packages/markovian-continuous/src/Markovian/Continuous/Measure/Exact.hs`, with private report storage and machinery in `Internal.hs`. Public accessors are ordinary projection functions rather than exported record labels: neither constructor use nor record update can forge the opaque report. The new operation reuses expansion/evaluation under one private meter; a private work-first mode preserves the older bivariate operation's successful accounting and failure precedence. The report performs saturated structural admission, canonical-owner/shared-interval checks, input/support observation, five ordered moments, eleven ordered derived rational operations and one comparison. Accessors perform no unmetered arithmetic and every failure is atomic. No coupling constructor, trajectory/policy framework, dependency edge or floating execution was added.
- Added `test/PairedDifference.hs`: eight direct affine-uniform oracle fixtures cover positive, zero and negative covariance (including non-independent zero covariance), shared/disjoint owners, public `shareAffineSource`/`independentPair`, signed coefficients, deterministic/zero laws and reversed construction input order. The oracle independently computes the difference variance from squared difference coefficients. All nine Rational fields and comparison are checked. The signed fixture's full golden is work **1234**, moments **5**, derived units **12**, raw pairs **81**, maximum canonical terms **10**, maximum rational bits **13**. Simultaneously exact limits pass; one-below each applicable limit fails. Every positive work prefix 1–1233 fails at its saturated next unit, including every derived step. Raw-pair, canonical and rational prefixes are also exhaustively checked for this fixture. Tests cover cancellation, discarded support/moment growth, derived-only bit growth and work-before-bit precedence, positive-limit/machine rejection, degree/term/preflight order and raw-before-work order.
- Added `test/PairedDifferencePrivate.hs`, compiled only against private source by the existing boundary script, for bounded infinite spines, duplicate/out-of-order owner rejection, inconsistent intervals, ownership-before-rational precedence and saturated preflight compared with an independent Integer formula up to machine-sized limits. Two separate compile-fail files prove constructor and record-update opacity. No private module was exposed or added to the installed test dependency graph. Existing nominal-owner/coordinate/space checks still run in the same script.
- Added `docs/book/src/paired-difference.md` and generated `docs/learning/paired-output.txt` through actual execution. U versus U reports difference variance **0**; U versus 1-U reports covariance **-1/12**, difference variance **1/3**, independent baseline **1/6**, and positive excess **1/6**. Independent uniforms report actual/baseline **1/6**, zero covariance/excess and EQ. The lesson states prerequisites, outcomes, exercises, independently checked answers and the common-noise-worsens-variance boundary. Teaching now checks **74** fences: four runnable contexts, 67 compiled fragments and three explicit pseudocode sketches; four executed output files are receipt-pinned. No existing fence was reclassified.
- Updated the current capability to unreleased implementation-fixtures while retaining EL-03 Proposed. The capability checker now validates implemented EL proposals against their separately retained frozen contracts; contract text cannot substitute for implementation evidence, and implementation cannot silently claim release or acceptance. Added seven regression tests (27 total), including archive-only missing-contract rejection. The current API map, generated capability presentation, package/root changelogs and source membership are updated. The public module-membership snapshots need no byte change because no module was added or removed; manifest/capability gates validated them. All three older continuous accounting goldens are unchanged. The frozen contract received evidence/status prose only, not a changed declaration or algorithm.
- Compiler/tests: `cabal build all --project-file=cabal.project.ci` and `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed on GHC 9.8.4 with warning-as-error project settings: **18** suites and **277** `PASS:` labels (`/tmp/el03-build.log`, `/tmp/el03-all-tests.log`). The focused continuous suite and final opacity/private probes passed (`/tmp/el03-focused.log`, `/tmp/el03-boundary-final.log`). Editor diagnostics incorrectly reported missing `Eq`/`Show SpaceError`; the source derives both, and a fresh compilation of the exact flagged `Main.hs` calls passed (`/tmp/el03-editor-diagnostic-verification.log`). Editor analysis is inconclusive, not a clean gate; no cache rebuild or source workaround was used.
- Teaching/presentation: `python3 scripts/check-learning --write`, then `--run`, passed context compilation and all four output comparisons (`/tmp/el03-learning-write.log`, `/tmp/el03-learning-run.log`); `python3 scripts/test_learning.py` passed **34** tests, including expected asserted compiler/runtime failures (`/tmp/el03-learning-tests.log`). `bash scripts/check-book` passed **49** book files, local links/includes and **420** source-to-generated display-math blocks (`/tmp/el03-book-final.log`). This is the existing HTML/MathJax semantic gate, not a new browser-startup claim.
- Documentation: followed the repository's warning-enabled isolated install procedure for the changed continuous package using `/tmp/el03-doc-store`, `/tmp/el03-doc-build` and `/tmp/el03-doc-environment`; `check-haddock-log` passed `/tmp/el03-haddock-install.log`, and the installed single-package interface check passed. Inspected the generated `Markovian-Continuous-Measure-Exact.html`, checked all twelve new operation/accessor anchors and the operation signature's exclusively public types. This is scoped documentation evidence, not the final all-package EL-07 Haddock/coverage campaign.
- Hygiene/metadata: focused Fourmolu, HLint (no hints), cabal-fmt, ShellCheck and shell syntax passed; Ruff and Python syntax passed for the changed capability scripts. Package manifest, release metadata and release policy passed (16 packages, 18 suites, 11 benchmarks; two policy tests); **27** capability tests plus immutable-release verification and **32** release-tool tests passed. Logs: `/tmp/el03-hlint.log`, `/tmp/el03-manifest.log`, `/tmp/el03-metadata.log`, `/tmp/el03-policy.log`, `/tmp/el03-capability-tests.log`, `/tmp/el03-release-tests.log`. An explicit Git/Python invariant check confirmed all 16 package versions, D-077–D-085 Proposed statuses, immutable tag object/target, current module-membership snapshots and previous continuous goldens unchanged (`/tmp/el03-invariants.log`).
- Archives: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el03-sdist` produced all **16** development archives. `python3 scripts/check-learning-archive dist-newstyle/el03-sdist` passed archive-only context compilation, all four executed outputs and all 34 checker tests (`/tmp/el03-sdist.log`, `/tmp/el03-archive-learning.log`). A separate validated archive extraction passed the archived capability checker and all 27 tests, then built/tested the standalone continuous archive with `--ignore-project --ghc-options=-Werror` and executed its archived opacity/private-invariant script (`/tmp/el03-archive-semantic.log`). No Git or checkout links supplied archive inputs. Artifacts: `dist-newstyle/el03-sdist/Markovian-2026.9.3.0.tar.gz` and `dist-newstyle/el03-sdist/markovian-continuous-2026.9.3.0.tar.gz`. After evidence-only checklist edits, refreshed all archives and checked byte-for-byte membership of all **26** intended changed/new distributed paths (`/tmp/el03-archive-membership.log`); executable inputs remain the validated ones.
- Failures resolved rather than hidden: initial golden bootstrap intentionally failed until the measured ledger was retained; a combined opacity fixture stopped at a rename-stage record-field error before reporting the constructor, so the checks were separated and both rerun; the capability gate initially rejected the newly implemented EL proposal and was repaired with the seven regressions above; teaching generation initially lacked the new output include, then created it through execution; two HLint unary-negation hints were fixed. A read-only Haddock signature inspector initially failed on HTML whitespace and passed after whitespace normalization. An initial wrong chapter filename was corrected during inspection. The first archive-membership inspector treated a Path as a string and failed before assertions; the corrected inspector passed all 26 paths. No failing validation result is counted as passing evidence.
- Preserved the pre-existing unrelated formatting edit in `scripts/test_learning.py` outside the intended EL-03 commit. No harness setting/package, publication, push, PR, merge, tag, version or decision-acceptance change occurred. EL-03 implementation review remains mandatory; EL-04–EL-06 and final EL-07 integration remain open. No GHC 9.4.8, CUDA/hardware, benchmark, browser-startup, hosted-CI or all-package final Haddock result is inferred from this stage. Structured writer artifact: `/home/josephcox/.pi/agent/sessions/--home-josephcox-dev-Markovian--/subagent-artifacts/outputs/4fb03c6b-8d5b-4487-9da4-397b39aa5f27/stages-v4/el03-paired-write.md`.

### 2026-09-07 — EL-03 review recorded; EL-04 fixed-topology reward JVP

- Supplied independent EL-03 review verdict: **passed**, high confidence. It found the mathematics, cumulative accounting, ownership, independent oracle and teaching sound; inspected logs substantiated execution but commands were not independently rerun. Its non-blocking P2 found that `PairedDifferenceBoundary.hs` assigned an eleven-argument constructor without applying it, allowing an arity error to masquerade as opacity evidence. Fully applied it with nine Rational zeros, `EQ` and a typed-by-context accounting field. The current boundary script passes (`/tmp/el04-continuous-boundary-final.log`). In an isolated copied package, deliberately exporting the constructor makes the exact same script fail with `Paired report constructor or record update escaped.` (`/tmp/el04-paired-mutation-build.log`, `/tmp/el04-paired-mutation-gate.log`). The checkout API remains opaque.
- Implemented all frozen EL-04 declarations in `src/Markovian/Feedback/Value/Exact.hs`. The real family is `r(e)+t*h(s,e)` at rational data, with fixed probabilities, strict discount, routes, event slots and layouts. Signed directions and zero-probability slots are admitted without changing reward/successor correlation or timing. The result is opaque with four nominal roles and jointly owns its base/derivative coefficients. Accessors project retained coefficients, never run new arithmetic. No probability/discount/policy tangent, general autodiff, full Jacobian, package, component or dependency edge was added.
- Extracted the existing metered base construction without changing its public call order or old accounting goldens. Private meter error adaptation and a prefix snapshot reuse one cumulative state, not separately reset public calls. The JVP reserves the frozen semantic cell/graph plans, checks base targets/layout then exact direction source/full-event layouts, observes all inputs, executes the base prefix, aggregates every direction cell, performs the deterministic one-RHS solve, derives external A in the frozen scale/multiply/add order, checks four derivative equation families literally, and observes retained A/zero K. Together with the base this is eight families, including empty families. Work failure precedes arithmetic-result bit failure and no partial result/account escapes.
- Added `test/FeedbackRewardJVP.hs`: eight fixtures cover signed rewards/directions, multiple sources, two loops and outputs, multiple event rewards on a shared route, zero probability, empty sources/loops/outputs, zero direction, gamma=0 and gamma=999/1000. An independent 0/1/2-dimensional determinant oracle checks base and derivative constants plus base continuation coefficients; an event-level dual-number finite unrolling independently checks discounted error bounds without production aggregation or solves. Consistent layout permutations preserve coefficients; source-only/event-only/reward-label changes reject. Absent labels return Nothing. Tests cover input observation and semantic failure order, ignored zero-mass direction growth, discarded Gaussian and cancelled derivative-product growth, and work-before-bit precedence.
- Literal full-account goldens: retry **24 cells, 52 graph visits, 78 arithmetic units, four maximum bits**; two-loop elimination **49 cells, 120 graph visits, 202 arithmetic units, seven maximum bits** with only three retained-result bits. Simultaneously exact limits pass; each one-below dimension/cell/graph/rational limit fails. Every arithmetic allowance 0–77 and 0–201 is checked for its exact next-unit failure, spanning both base and derivative phases. Zero h and gamma do not shortcut the 78-unit plan. `test/FeedbackRewardJVPPrivate.hs` compares actual private reservations with an independent Integer oracle across **6561** count combinations up to machine size, including intermediate product-prefix overflow.
- Added separate constructor-opacity and nominal-role compile-fail files. The four role probes use representationally equal newtypes, not unrelated primitive types. `scripts/check-feedback-boundary` uses the exact current root inplace package and separately compiles/runs the private reservation probe. It passes (`/tmp/el04-boundary-final.log`); the archived script also passes. The JVP constructor probe compiles when the constructor is deliberately exported in an isolated copied source tree (`/tmp/el04-jvp-opacity-mutation.log`). Editor errors on the four coercions are **expected negative-test evidence**, not production blockers; changing them to compile would remove the ownership gate. Production source and tests compile with GHC 9.8.4 warnings treated as errors; no cache rebuild or harness change was used.
- Added `docs/book/src/reward-jvp.md`, linked it from feedback, API map, navigation and differentiation route, and updated current context, changelog and capability records/presentation. The API retry gives V=4/3 and **dV/da=4/3**. **dV/dp=8/9** is explicitly a separately evaluated symbolic exercise outside this API. Independent four-event dual unrolling gives `(V4,dV4/da)=(85/64,85/64)`, with derivative error 1/192; the separately symbolic finite probability derivative is 27/32, not 8/9. Gamma-zero answers are asserted. Displayed stdout is generated by the executed fixture, not hand-entered.
- Compiler/tests: `cabal build all --project-file=cabal.project.ci` and `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed **18 suites**, **282 PASS labels** (`/tmp/el04-build.log`, `/tmp/el04-all-tests.log`). Focused root tests also passed. Existing affine and other feedback accounting goldens remain byte-identical. Later source edits only clarify the Gaussian caller comment; teaching compilation and the final archive refresh validate those bytes.
- Teaching/presentation: `python3 scripts/check-learning --write`, then `--run`, passed compilation and all **five** output comparisons; **75** Haskell fences are five runnable, 67 compiled fragments and three explicit pseudocode sketches. Final logs: `/tmp/el04-learning-write-final.log`, `/tmp/el04-learning-run-final.log`. All **34** checker negative tests passed (`/tmp/el04-learning-tests.log`). `bash scripts/check-book` passed **50** book files, local links/includes and **422** source-to-generated MathJax display blocks (`/tmp/el04-book-final.log`); no new browser-startup claim is made.
- Capability tests now construct a stable explicit contract-only fixture instead of assuming the reward JVP is still unimplemented, and add transition/release/evidence regressions. All **30** tests and immutable-release capability verification passed (`/tmp/el04-capability-tests.log`, `/tmp/el04-capabilities.log`); all **32** release-tool tests passed (`/tmp/el04-release-tests.log`). Focused Fourmolu, HLint (no hints), cabal-fmt, Ruff, Python syntax, ShellCheck, shell syntax, `cabal check`, root-topology, package-manifest, release-metadata, release-policy (two tests), and `git diff --check` passed. Logs include `/tmp/el04-hlint.log`, `/tmp/el04-fourmolu.log`, `/tmp/el04-root-topology.log`, `/tmp/el04-manifest.log`, `/tmp/el04-metadata.log`, `/tmp/el04-policy.log`. Manifest counts and all 16 package versions/edges are unchanged; an explicit check also covered the example Cabal version. D-077–D-085 remain Proposed, the released tag object/target and current module-membership snapshots are unchanged. No module was added or removed, so snapshots require no byte change.
- Scoped documentation: warning-enabled `cabal --store-dir=/tmp/el04-doc-store install --lib Markovian --project-file=cabal.project.ci --builddir=/tmp/el04-doc-build --package-env=/tmp/el04-doc-environment --enable-documentation --overwrite-policy=always` passed. `check-haddock-log` accepted `/tmp/el04-haddock-install.log`; the root-only installed-interface check passed. Inspected all five new operation/accessor signatures in installed `Markovian-Feedback-Value-Exact.html`: only public types occur. This is scoped root evidence, not the all-package final EL-07 documentation/coverage campaign.
- Archives: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el04-sdist` produced all **16** development archives. `python3 scripts/check-learning-archive dist-newstyle/el04-sdist` passed archive-only context builds, all five executed comparisons and all 34 checker tests (`/tmp/el04-sdist.log`, `/tmp/el04-archive-learning.log`). A separately validated all-package archive extraction passed archived capability checking and all 30 tests (`/tmp/el04-archive-capabilities.log`). A validated standalone root extraction built/tested the full root suite with a base-only archive-local project and `-Werror`, then ran its archived feedback opacity/role/private probes (`/tmp/el04-archive-root-tests.log`, `/tmp/el04-archive-root-boundary.log`). No Git or checkout symlinks supplied those inputs. Artifacts: `dist-newstyle/el04-sdist/Markovian-2026.9.3.0.tar.gz` and the other 15 package archives; refreshed after final evidence/comment edits and checked byte-for-byte against all **25** intended changed/new distributed paths (`/tmp/el04-archive-membership.log`). Final full build/test and archive-only teaching reruns also passed: `/tmp/el04-build-final.log`, `/tmp/el04-all-tests-final.log` (18 suites, 282 PASS labels), `/tmp/el04-sdist-final.log`, `/tmp/el04-archive-learning-final.log` (five outputs, 34 checker tests).
- Resolved failures are not passing evidence: the first test compilation rejected partial `head`, which was replaced by explicit matching; combined constructor/role probes stopped before role errors, so they were separated; the first private oracle omitted the existing helpers' overflowing product prefixes before a later zero, and was corrected against that specified machine-saturating order; five HLint patterns and two further hints were fixed; capability tests initially assumed the old unimplemented row and one new test expected the wrong release-error precedence, both corrected; teaching generation first required the new output include to exist, then generated its content by execution; copied-package mutation initially used unsupported `cabal build --ignore-project`, then used a standalone `cabal build` successfully; the first archive-membership inspector treated a Path as a string, failed before assertions, and was corrected before the 25-path check passed. The expected malformed Haskell/runtime negative fixtures and constructor-export mutation failures are asserted gate behavior, not hidden compiler failures.
- Preserved the pre-existing unrelated `scripts/test_learning.py` formatting edit outside this intended commit. Independent EL-04 correctness/teaching review remains required; EL-05, EL-06 and final EL-07 stay open. No GHC 9.4.8/lower-bounds, GPU/CUDA/hardware, benchmarks, hosted CI, browser-startup, final all-package Haddock, publication, push, PR, merge, tag, version or acceptance action is claimed. No nested delegation or harness settings/packages changed. Writer artifact: `/home/josephcox/.pi/agent/sessions/--home-josephcox-dev-Markovian--/subagent-artifacts/outputs/4fb03c6b-8d5b-4487-9da4-397b39aa5f27/stages-v4/el04-feedback-write.md`.

### 2026-09-07 — EL-04 review recorded; EL-05 supplied-partition aggregation

- Received the supplied independent EL-04 verdict: **passed**, high confidence, no findings. The reviewer checked the frozen contract, equations, ownership, saturated reservations, cumulative rational accounting, determinant/event-level oracles and bounded teaching claims. Inspected logs substantiated execution; the reviewer did not independently rerun commands. This closes EL-04 review, not EL-05 review or final integration.
- Implemented the complete frozen EL-05 public surface in `src/Markovian/Aggregation/Exact.hs`, in the exact root with no package/dependency edge. The checker takes an already compiled, closed policy and never calls model/policy closures or recompiles a quotient. It returns either an opaque jointly owned original-layout/mapping/quotient table or an opaque first distinguishing witness. State, block and observation roles are nominal on both witnesses and the result. Observers project retained data; none evaluates rationals or exposes record updates.
- The unchanged contract requires exact observation equality, terminal kind, applicable terminal payoff, and the **joint** immediate-reward/next-block law, with canonical ascending reward/block-ordinal atoms and duplicate mass addition. Partition and observation spines are separately bounded before semantic validation; missing/duplicate/unknown/unused errors and machine/structural/rational precedence follow the frozen order. All original canonical rows are completed before comparison. One meter covers structural reservation, input/result observations, insertion/addition, representative comparisons, and either witness construction/re-observation/literal revalidation or quotient/mapping retention. `Left` exposes no partial accounting or evidence. Self-review removed redundant equality joins: coverage checks also produce canonical lists, collection uses zip, and shared nonempty block groups avoid repeated representative scans. This preserves the frozen work charge and error order while keeping collection within its conservative reservation.
- Added `test/AggregationExact.hs` to the existing root suite. Independent finite recursion over public quotient rows agrees with `evaluateCompiledExactFinite` at six horizons and three discounts, for original/reversed layouts and signed rewards/payoffs. Separate raw-source and quotient path enumerators agree on complete reward/observation/terminal-versus-horizon laws; equality-class summation does not reuse production insertion or aggregation. Fixtures reject equal means with different reward laws, equal separate marginals with opposite reward/successor correlation, terminal kind, payoff and observation mismatches. Tests assert canonical block/mapping order, first representative/member/witness order, reversed valid associations, unequal/missing joint masses, bounded infinite supplied lists, malformed/combined-invalid precedence, and all-original-input/canonicalization-before-witness behavior.
- Exact success golden: **4 states, 2 blocks, 5 raw atoms, 4 original canonical atoms, 162 work units, 3 maximum rational bits**. Structural W0 is 121, followed by 13 input observations, nine insertion/addition units, four retained masses, nine pair comparisons and six retained rows. Terminal-only zero-support golden is **1/1/0/0/12/1** in accounting field order. Simultaneously exact limits and every smaller work prefix are checked for success, all witness families and duplicate-growth fixtures; every applicable one-below state/block/support/rational limit fails atomically. Transient `1/3+1/4=7/12` grows to four bits before a retained unit mass; work exhaustion precedes that bit failure. A later row's discarded growth fails before an earlier block's potential payoff witness.
- Added four compile-fail files for both constructor probes, nine nominal roles, quotient record update and witness record update. `scripts/check-aggregation-boundary` uses the exact current root inplace unit and also checks **480** saturated W0 cases against an independent unbounded Integer formula in private source context, without exposing helpers. Source and final archive boundary scripts pass (`/tmp/el05-boundary-final.log`, `/tmp/el05-archive-root-boundary-final.log`). The fully applied constructor probe **compiles** when both constructors are deliberately exported in an isolated copied source tree (`/tmp/el05-opacity-mutation.log`), proving that the negative is opacity rather than an arity/type defect. The checkout remains opaque. Source CI, archive CI and ShellCheck include the new gate.
- Added `docs/book/src/aggregation.md`, MDP-route/navigation/API-map links and an executed output artifact. The lesson successfully merges states 20/30 and 10/11 while preserving raw duplicate-law differences, computes live finite-horizon values **0, 11/4, 55/16**, preserves terminal payoff **7 at horizon zero**, and reports the mean-only failed merge's reward-0/block-1 masses **1/2 versus 0**. Prerequisites, outcomes, exercises, checked answers, trace timing and theorem exclusions are explicit. No partition search, alternative-policy/action-trace preservation, general MDP/POMDP abstraction, novelty or performance claim was added.
- Updated the current root module-membership snapshot for the one new exposed module, changelog, capability record/generated presentation, current context, source membership and contract evidence/status prose. Frozen declarations/algorithm and Proposed status are unchanged. Three capability regressions check the implemented EL-05 transition, rejection of release claims and rejection of contract-only evidence substitution; **33** capability tests and immutable-release checking pass (`/tmp/el05-capability-tests-final.log`, `/tmp/el05-capabilities-final.log`). Existing immutable released-module records remain unchanged.
- Compiler/tests: `cabal build all --project-file=cabal.project.ci` and `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed on GHC 9.8.4 with warning-as-error settings: **18 suites, 288 PASS labels** (`/tmp/el05-build-final.log`, `/tmp/el05-all-tests-final.log`). Existing feedback and other goldens are unchanged. Initial editor analysis was unavailable; later editor responses reported clean production/test files, but actual GHC compilation is the authoritative evidence. No cache rebuild was used to silence diagnostics.
- Teaching/presentation: `python3 scripts/check-learning --write`, then `--run`, passed compilation and all **six** output comparisons; **76** fences are six runnable contexts, 67 compiled fragments and three explicitly classified sketches. Logs: `/tmp/el05-learning-write-final.log`, `/tmp/el05-learning-run-final.log`. All **34** checker tests passed (`/tmp/el05-learning-tests.log`), including asserted compiler/runtime rejections. `bash scripts/check-book` passed **51** book files, local links/includes, capability/learning checks and **422** source-to-generated MathJax display blocks (`/tmp/el05-book-final.log`), not a new browser-startup claim.
- Hygiene/metadata: focused Fourmolu, HLint (no hints), cabal-fmt, Ruff/Python syntax, ShellCheck/shell syntax, `cabal check` and `git diff --check` passed. Package manifest, release metadata, release policy (two tests), root-topology and all **32** release-tool tests passed (`/tmp/el05-check-package-manifest.log`, `/tmp/el05-check-release-metadata.log`, `/tmp/el05-check-release-policy.log`, `/tmp/el05-check-root-topology.log`, `/tmp/el05-release-tests.log`). Explicit invariants confirm all 16 package versions, D-077–D-085 Proposed, immutable tag object/target, historical release evidence and existing goldens unchanged (`/tmp/el05-invariants.log`).
- Scoped documentation: warning-enabled `cabal --store-dir=/tmp/el05-final-doc-store install --lib Markovian --project-file=cabal.project.ci --builddir=/tmp/el05-final-doc-build --package-env=/tmp/el05-final-doc-environment --enable-documentation --overwrite-policy=always` passed. `check-haddock-log` accepted `/tmp/el05-haddock-install-final.log` and the root-only installed-interface gate passed. Inspected all nine final public operation/accessor signatures in installed `Markovian-Aggregation-Exact.html`; none contains hidden Meter/Row/Atom/internal types (`/tmp/el05-haddock-signatures.log`). This is scoped root documentation evidence, not final all-package EL-07 coverage.
- Archives: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el05-sdist` produced all **16** development archives. Final `python3 scripts/check-learning-archive dist-newstyle/el05-sdist` passed archive-only context builds, all six executed comparisons and all 34 checker tests (`/tmp/el05-sdist-final.log`, `/tmp/el05-archive-learning-final.log`). A separately validated all-package extraction passed archived capability checks and all 33 tests (`/tmp/el05-archive-capabilities.log`). The final standalone root extraction built/tested its full suite under a base-only archive-local project with `-Werror`, then ran its archived opacity/role/preflight gate (`/tmp/el05-archive-root-tests-final.log`, `/tmp/el05-archive-root-boundary-final.log`); no Git or checkout symlinks supplied inputs. Root artifact: `dist-newstyle/el05-sdist/Markovian-2026.9.3.0.tar.gz`. After the evidence-only checklist edit, refreshed all 16 archives and checked byte-for-byte membership of all **25** intended changed/new distributed paths (`/tmp/el05-archive-membership.log`); executable inputs remain the validated final ones.
- Resolved failures are not passing evidence: initial test compilation found an unused import, and a later marginal oracle needed an explicit Rational annotation; both were fixed. An initial hand-computed golden missed two W0 units and one insertion unit; it was corrected with the breakdown above. HLint's two hints were fixed. GHC reports hidden same-named constructors as illegal term-level type-constructor use and stops early on missing record fields; the gate now checks actual diagnostic categories and separates the two record-update probes. An attempt to reuse an already populated install environment failed Cabal-7145; final changed-source documentation was validated with fresh isolated install paths, not by forcing stale evidence. An initial decision invariant regex assumed two-heading markers; corrected to the actual three-heading markers and reran successfully. Expected negative-test compiler/runtime failures and the deliberate constructor-export mutation are asserted evidence, not hidden gate failures.
- Preserved the pre-existing unrelated `scripts/test_learning.py` formatting edit outside the intended EL-05 commit. No harness settings/packages, package edge, version, decision acceptance, publication, push, PR, merge, tag or nested delegation operation changed. Independent EL-05 correctness/teaching review is required; EL-06 and final EL-07 remain unchecked. No lower-bounds/GHC 9.4.8, GPU/CUDA/hardware, benchmark, hosted CI, browser-startup or final all-package Haddock result is inferred. Writer artifact: `/home/josephcox/.pi/agent/sessions/--home-josephcox-dev-Markovian--/subagent-artifacts/outputs/4fb03c6b-8d5b-4487-9da4-397b39aa5f27/stages-v4/el05-aggregation-write.md`.

### 2026-09-07 — EL-05 review recorded; EL-06 source-semantic resource admission

- Received the supplied independent EL-05 verdict: **passed**, high confidence, no findings. It reviewed the frozen aggregation contract, values/traces, canonical layouts, ownership, joint laws, rational accounting and bounded atomic witnesses. Inspected logs substantiated execution; reviewer commands were not independently rerun. This closes EL-05 review, not EL-06 review or final EL-07.
- Implemented the entire bounded EL-06 experiment and pending EL-02 resource lesson in `test/ResourceAdmission.hs`, registered in the existing root suite with `--resource-admission`. No library source, public API, component, package, dependency edge or module-membership snapshot changed. The closed last-write scalar model is **not a cache or retained-circuit interpreter**: every path runs the same source checks; executor costs are separately declared assumptions, not measured work. It contains no callback, cached entry, lookup, identity certification or trace-compression implementation.
- Expanded the frozen EL-06 policy and consistently amended **Proposed** D-085. Source work, literal denotation, source error precedence and executor admission are separate. Infrastructure preflight occurs first; source structure then ordered unit-work/numerator/denominator/primitive checks follow. Constructor costs are n semantic and n+2 declared executor units, a hypothetical hit n semantic and one declared executor unit inclusive of assumed replay. Actual replay is not free and no real one-unit replay is demonstrated. All semantic limits are key fields; changed limits miss. Only complete successful tables could be retained in a future cache; pure deterministic terminating callbacks require explicit stable identity and replayable bounded evidence, never closure or label equality. Future concrete cache signatures and performance measurements remain D-085 work.
- Checked source **100** versus hypothetical executor **1** under semantic budget **10**: both reject at source charge **11**. At semantic 100 both return exact 1 with source work 100 and distinct executor reservations 100/1. Executor budget ten distinguishes infrastructure admission instead. A real entry built at semantic 100 would miss at ten; success-only retention cannot have produced an entry at ten. Construction plus hit shares one operation-wide source/executor account: **200/103**, with exact one-below failures, not a reset meter or double-counted construction. Rational maxima persist across occurrences.
- Added five named resource checks. All three paths exercise every source-work prefix 0–99, exact and one-below source structure, occurrence, trace, executor and rational limits, machine/invalid-limit precedence, bounded infinite and empty plans, saturated reservations (80 independent Integer-oracle cases), numerator-before-denominator and work-before-rational/primitive precedence. Two traces with identical final zero, work three and maximum denominator nine fail differently at work one/denominator eight: denominator nine versus work two. Discarded numerator/denominator 257/256 is checked before retaining zero. `Left` exposes no partial values/account. Tests assert independent literal expected values/failures, not reference-versus-hit equality alone.
- The real `bindExactFiniteDistChecked` contrast returns literal four-path entries `[(0,1/4),(3,1/4),(3,1/4),(6,1/4)]` when admitted. Left per-call work is **12,16**; right outer/inner/inner work is **12,8,8**. Under local work 12, left fails at its second call's work 13 while right succeeds. The lesson explicitly distinguishes these existing separately budgeted public calls from the model's cumulative request meter. Equal summed work 28 does not imply equal local admission, a Monad instance or a universal optimizer law.
- Added `docs/book/src/resource-admission.md` as laboratory G, linked from the laboratory, probability/composition route and navigation; updated changelog and generated `docs/learning/resource-admission-output.txt` plus receipt by execution. The manifest now classifies **77** Haskell fences: seven runnable, 67 compiled fragments and three explicit sketches, with **seven** executed output files. Existing source CI and archive CI discover the new module/run through that manifest; no duplicate runner, new protocol, harness settings/package change or CI-specific bypass was added. The whole module is automatically in the root source archive through Cabal, and existing document/output globs cover the new lesson/output.
- Compiler/tests: final `cabal build all --project-file=cabal.project.ci` and `cabal test all --project-file=cabal.project.ci --test-show-details=direct` passed GHC 9.8.4 warning-as-error compilation, **18 suites** and **292 line-start PASS labels**, including all five resource checks (`/tmp/el06-build-final.log`, `/tmp/el06-all-tests-final.log`). Focused root tests also passed (`/tmp/el06-focused.log`). Initial editor analysis was unavailable; later responses were clean, but actual compiler output is authoritative. No cache rebuild or source workaround was used.
- Teaching/presentation: final `python3 scripts/check-learning --write`, then `--run`, passed all context compilation and seven output comparisons (`/tmp/el06-learning-write-final.log`, `/tmp/el06-learning-run-final.log`). All **34** checker tests passed, including expected asserted compiler/runtime rejections (`/tmp/el06-learning-tests.log`). `bash scripts/check-book` passed **52** book files, local links/includes, capability/learning checks and **422** source-to-generated MathJax display blocks (`/tmp/el06-book-final.log`). This is not a browser-startup claim.
- Hygiene/metadata: focused Fourmolu, HLint (no hints), cabal-fmt, `cabal check` and `git diff --check` passed. Package manifest, release metadata, release policy (two tests) and root topology passed (`/tmp/el06-check-package-manifest.log`, `/tmp/el06-check-release-metadata.log`, `/tmp/el06-check-release-policy.log`, `/tmp/el06-check-root-topology.log`). Immutable-release capability verification and all **33** capability tests passed (`/tmp/el06-capabilities.log`, `/tmp/el06-capability-tests.log`); all **32** release-tool tests passed (`/tmp/el06-release-tests.log`). Explicit Git/Python checks confirmed all tracked Cabal versions, Proposed D-077–D-085, released tag object/target, library sources, package edges and public snapshots unchanged.
- Archives: `cabal sdist all --project-file=cabal.project.ci --output-directory=dist-newstyle/el06-sdist` produced all **16** development archives (`/tmp/el06-sdist.log`). `python3 scripts/check-learning-archive dist-newstyle/el06-sdist` passed archived context compilation, all seven executions/output comparisons and all 34 checker tests without Git or checkout symlinks (`/tmp/el06-archive-learning.log`). A separate validated standalone root extraction compiled/tested the entire root suite with a base-only temporary project and `--ghc-options=-Werror`, including all five resource checks (`/tmp/el06-archive-root-tests.log`). Root artifact: `dist-newstyle/el06-sdist/Markovian-2026.9.3.0.tar.gz`. After the evidence-only log edit, refreshed all 16 archives and checked byte-for-byte membership of all **14** intended distributed paths; executable inputs remain the validated ones.
- Resolved failures/skips: the first focused HLint run reported five hints (discarded fold results, tuple mapping, void and eta reduction); repaired them and reran successfully (`/tmp/el06-hlint-final.log`). Markdown autofix briefly interpreted a line-start `1.` as a list; rephrased and the final book gate passed. No actual compiler/test failure from the new implementation is hidden. Existing intentional compiler/runtime negatives are asserted checker evidence. No new public Haddock or compile-fail campaign was run because no library/ownership/public surface changed; final EL-07 still owns full documentation/boundary integration. No GHC 9.4.8/lower-bounds, CUDA/hardware, benchmarks, browser startup or hosted-CI result is claimed.
- Preserved the pre-existing unrelated formatting edit in `scripts/test_learning.py` outside this intended commit. All EL-06 implementation items and EL-02's resource lesson are now evidenced; independent EL-06 correctness/teaching review and final EL-07 remain outstanding. No publication, push, PR, merge, tag, version, decision acceptance or nested delegation occurred. Writer artifact: `/home/josephcox/.pi/agent/sessions/--home-josephcox-dev-Markovian--/subagent-artifacts/outputs/4fb03c6b-8d5b-4487-9da4-397b39aa5f27/stages-v4/el06-resource-write.md`.

### 2026-09-07 — EL-06 review recorded; EL-07 local integration

- Received the supplied independent EL-06 **passed** verdict, no findings, high confidence. It reviewed the contract/model, real checked-bind contrast, independent literal/Integer oracles, lesson and archive wiring; inspected logs substantiated execution, but commands/Git invariants were not independently rerun. This closes EL-06 review, not final EL-07 review.
- Audited every requirement against the phase artifacts and fresh integration gates. Detailed commands, outcomes, logs, archive membership and honest exclusions are in [the shipped integration record](../evidence/EXECUTABLE-LEARNING-INTEGRATION.md). EL-00–EL-06 remain evidenced. Final independent correctness/teaching review remains unchecked; this writer does not self-award it.
- Fixed integration gaps: CI now explicitly builds all tests and benchmarks together and syntax-checks both capability scripts; the archive-only teaching gate now also invokes archived capability checking and all its negatives; repaired one existing Ruff nested-condition hint in the link checker. Added the durable integration record to root extra-doc membership, then regenerated the teaching receipt by execution. No library semantics, public signatures, package edges, versions, release history or proposal status changed. The unrelated pre-existing `scripts/test_learning.py` formatting diff is preserved outside the intended commit.
- Actual GHC 9.8.4 results: full `--enable-tests --enable-benchmarks` warning-as-error build passed; all **18** test suites passed (**293** line-start PASS labels, not a total assertion count); all **11** host benchmark components finished. Prescribed CI `-Wunused-packages` build passed separately. All 19 CI boundary/profile scripts and root-topology passed; an additional isolated exact-current-inplace replay rejected all **54 tracked** Haskell negative fixtures. Logs: `/tmp/el07-build.log`, `/tmp/el07-tests.log`, `/tmp/el07-benchmarks.log`, `/tmp/el07-unused-ci.log`, `/tmp/el07-boundaries.log`, `/tmp/el07-exact-boundaries-current.log`.
- All source-format/Python/shell/metadata gates passed; **32** release-tool, **33** capability, **34** learning, two policy and 12 CUDA-profile Python tests passed. Full teaching compilation and all **seven** executed outputs passed; **77** fences remain seven runnable, 67 compiled fragments and three explicit sketches. Book/link/static-math gates passed **52** Markdown files and **422** display blocks. Actual already-installed Chromium startup/DOM inspection passed **51** generated chapter pages and **422** SVG math containers, with no MathJax error nodes; no visual/cross-browser/hosted claim or runtime installation is inferred. Logs are indexed in the integration record.
- Authoritative warning-enabled isolated Haddock installation passed warning-log validation and interfaces for **all 16 packages**. Inspected **26** new operation/accessor signatures: public types only. The separate warning-suppressed coverage-only pass checked **133** public modules at 100%; it is not used as warning evidence. Logs: `/tmp/el07-haddock-install.log`, `/tmp/el07-public-signatures.log`, `/tmp/el07-haddock-coverage.log`.
- All **16** source archives passed bounded validation. Archived tools compiled contexts, checked all seven executed outputs and passed all 33/34 capability/learning negatives without Git/checkout links. Separately, all 16 reconstructed archive packages passed warning-enabled full build, **18** test suites, EL-03/04/05 exact-unit boundaries/private probes, root topology and seven output comparisons using shipped projects/sources/records/scripts. Final archives include byte-identical contents of all **106** changed/new distributed paths since the baseline, including this checklist and the integration record. Logs: `/tmp/el07-archive-learning.log`, `/tmp/el07-archive-full.log`, `/tmp/el07-archive-membership-final.log`. Root artifact: `dist-newstyle/el07-sdist/Markovian-2026.9.3.0.tar.gz`.
- Failures/limits are explicit: an optional expanded unused-package check covering every benchmark rejected pre-existing direct root dependencies in two inventory benchmark components; required full warning build and prescribed CI unused-package scope passed independently. No topology change or bogus import was made to silence it. Initial Ruff, temporary-environment lifetime, overbroad ignored-archive inventory and Markdown status-regex issues were corrected and rerun; only final successful checks count. GHC 9.4.8, preferred-oldest, CUDA-enabled hardware/sanitizers, hosted CI and publication remain unexecuted, not silently checked. Final independent EL-07 review and parent completion/optional benchmark-dependency policy audit remain next actions.
- Explicit invariant checks passed for all tracked versions/dependency declarations, Proposed D-077–D-085 and immutable released tag object/target/records (`/tmp/el07-invariants-final.log`). No harness settings/packages, new runtime dependency, push, PR, merge, tag, version, acceptance or nested delegation action occurred. Local intended changes are committed for parent review; external operations remain parent-controlled.

## Completion standard

This program is complete only when EL-00 through EL-07 have evidence for every applicable item. Do not replace implementation with plans. Do not mark a failed or unexecuted gate as passed. Scope changes require explicit rationale and supervisor review. Unsupported universal features remain excluded rather than becoming hidden obligations.
